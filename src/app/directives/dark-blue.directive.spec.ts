import { DarkBlueDirective } from './dark-blue.directive';

describe('DarkBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkBlueDirective();
    expect(directive).toBeTruthy();
  });
});
